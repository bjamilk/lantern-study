/**
 * The Record door's title helper, on web.
 *
 * The format lives in `@lantern/shared` so web and mobile cannot drift.
 * This file stays so existing imports keep working.
 */

export { newLectureNoteTitle } from '@lantern/shared';
