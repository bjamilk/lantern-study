import React from 'react';

// This component is no longer used as DMs are now integrated into the main chat window.
// It is kept temporarily to avoid breaking imports but will be removed in a future cleanup.
const DirectMessageModal: React.FC = () => {
  return null;
};

export default DirectMessageModal;