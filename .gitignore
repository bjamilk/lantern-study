# Logs
logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
**/dist/
.turbo/
*.apk
# Local iOS simulator build artifacts (packaged .app bundles)
*-iphonesimulator.tar.gz
*.local

# Environment variables
.env
.env.local
.env.render
.env.cloudflare
.env.resend
.env.sentry
.env.*.local
**/.env
!.env.example
!**/.env.example

# Local Supabase / migration tooling artifacts
.migration-payloads/
supabase/.temp/
supabase/.branches/

# Expo / mobile build output
apps/mobile/.expo/
apps/mobile/.expo-*/
apps/mobile/.diagnostic-export/
apps/mobile/dist/

# Responsive audit artifacts
tmp/
tmp/responsive/
playwright-report/
test-results/
**/playwright/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
# Committed so every editor gets the same warning about the root tsconfig (#75).
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Xcode / Swift
xcuserdata/
build/
DerivedData/
*.xcuserstate

# Agent worktrees / local session scratch
.claude/

# Local mobile build artifacts (never commit — large binaries)
apps/mobile/build-*.apk
apps/mobile/build-*.tar.gz
apps/mobile/build-ios-sim-*.tar.gz

# Local scratch / debugging artifacts (never commit)
tmp_*
temp_*
.tmp-home/
.wrangler/
*.tar.gz
