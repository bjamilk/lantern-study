/**
 * The Exam readiness card's model, for mobile.
 *
 * The rules themselves live in `@lantern/shared/learning` so the web card
 * renders the same ones — see `packages/shared/src/learning/readinessCard.ts`.
 * This module is the mobile import path (and the home of its test), not a
 * second implementation.
 */
export * from '@lantern/shared/learning/readinessCard';
