/**
 * Re-export of the shared Profile menu. The phone used to own this list;
 * web and mobile now render the same rows from @lantern/shared.
 */
export {
  buildMeSections,
  meProfileBlockOrder,
  meRowIds,
  type MeProfileBlockId,
  type MeRow,
  type MeRowId,
  type MeRowIcon,
  type MeRowKind,
  type MeSection,
  type MeState,
} from '@lantern/shared/learning';
