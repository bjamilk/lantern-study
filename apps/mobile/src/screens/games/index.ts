// Games screens barrel export
//
// The 1v1 quiz-battle lane: the live match (GameScreen), its scoreboard
// (GameResultScreen), and the pending-challenge inbox (ChallengesInboxScreen).
// Default exports only; the navigator registers each one by route name.
export { default as GameScreen } from './GameScreen';
export { default as GameResultScreen } from './GameResultScreen';
export { default as ChallengesInboxScreen } from './ChallengesInboxScreen';
